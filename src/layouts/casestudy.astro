---
import { TooltipProvider } from "@/components/ui/tooltip";
import { SidebarLayout } from "@/components/sidebar-layout";

import "../styles/global.css";

const { title, subtitle, heroImage } = Astro.props;
---

<SidebarLayout client:load title={title} baseUrl={import.meta.env.BASE_URL}>
  <TooltipProvider client:load>
    <div class="flex-1 flex items-center justify-center">
      <div class="max-w-4xl mx-auto px-6 py-16 w-full">
        <header class="mb-12 text-center">
          <h1 class="text-4xl font-bold mb-2 font-bebas">{title}</h1>

          {subtitle && <p class="text-lg text-muted-foreground">{subtitle}</p>}

          {
            heroImage && (
              <img
                src={heroImage}
                alt={title}
                class="mt-6 rounded-lg shadow-lg"
              />
            )
          }
        </header>

        <article
          class="mx-auto space-y-12 prose prose-neutral dark:prose-invert"
        >
          <slot />
        </article>
      </div>
    </div>
  </TooltipProvider>
</SidebarLayout>
